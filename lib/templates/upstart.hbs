description "{{appName}} node process"
start on runlevel [2]
stop on runlevel [016]
console owner
chdir "{{serverAppPath}}/{{appName}}"
env NODE_ENV={{nodeEnv}}
env PORT={{upstreamPort}}
exec nave use {{nodeVersion}} npm start >> /var/log/{{appName}}.log 2>&1
respawn
respawn limit 5 15
