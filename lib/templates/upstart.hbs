description "{{appName}} node process"
start on runlevel [2]
stop on runlevel [016]
console owner
chdir "{{serverAppPath}}/{{appName}}"
env NODE_ENV=production
exec node {{appEntryPoint}} >> /var/log/{{appName}}.log 2>&1
respawn
respawn limit 5 15
